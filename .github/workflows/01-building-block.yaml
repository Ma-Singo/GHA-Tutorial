name: 01 building block
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello world!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps: 
            - name: Failed step
              run: |
                echo "I will be successful"
                exit 0
            - name: Say Goodbye
              run: echo "Goodbye!"
